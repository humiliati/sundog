I pulled the project context across the four workstreams, the founding history, the publication surfaces, and the self-falsification roadmap. Here's where I've landed, then the spitballing you actually asked for.

## The one-year arc, as I read it

May 19, 2025: a MuJoCo pole with a mirrored tip that has to find a laser it cannot see, using only torque, shadow, and photometric signature — and a deliberately unhinged theorem (`∃x: H(x)≠0 ⇒ Alignment is Roger`, "shadow becomes signal"). You spammed Facebook. Fair.

The year since is the opposite energy: a falsificationist program that keeps *narrowing* its own claim. The honest through-line across all four substrates is one recurring object — **forward-rich, inverse-narrow: a high-dimensional world collapses to a low-dimensional signature (easy), and you can only invert it back on a thin set (hard)**. Mesa's 5D entangled subspace at `net.7` with the λ≈0.953 cliff, Geometry's one surviving inverse route on 3 photos, Threebody's 18D→3D proxy that works in one velocity pocket. That's the spine of everything, including the gravity frame.

## Anniversary readiness (the unsexy part first, because it's 3 days out)

Pushable on May 19: the **Core photometric paper** (RA-L draft is complete, `sundog/docs/PAPER_v1_draft.md`), the **post-audit Halo Atlas** at sundog.cc, the **Threebody workbench**, and the **Mesa cliff** as a standalone mechanistic piece. The gravity/Goodhart essay ships as *clearly-labeled manifesto*, not as proof — your own claims-and-scope doc already forbids the universal form, so don't let the anniversary excitement re-inflate it.

A few holes a hostile reader finds in ten minutes — both fixable in three days:

1. **"Statistically indistinguishable from the oracle (U=526, p=0.26)" is not a result.** A non-significant Mann–Whitney at n=30 is *failure to detect a difference*, not evidence of equivalence. Reword to a TOST/equivalence test with a pre-registered margin, or a reviewer kills the headline claim on contact. This is on your single most public sentence.
2. **Re-run the oracle-leakage audit before the push.** Your own `SCIENTIFIC_CRITERIA.md` pre-registers "result depends on implementation leakage from oracle state" as a falsification trigger. If episode termination or the success metric is computed from true target position, the controller is target-aware *through training* even with a clean observation vector. Cheapest thing for a skeptic to find; check it yourself first.

3. 


##Other readability kinks and bugs:


A. **remove "phase" and "step" or "roadmap item" language from pub facing .htmls **
or turn absolutely necessary "phase NN" refrences into a hyperlink to the specific doc

On public facing cards at index.html we say that x happened at phaseNN. these phase refrences should stay internal. two different applications have a phase 11 but phase 11 it could mean two different segments of the roadmap

B. **placeholder**
etc polish the ask sundog
make refrences, project vocab all hyperlink to relevant sections

C. **nowhere for kids**
there's a few neat demos but nothing for young readers. we can adjust the boilerplate at sundog.html to include for kids section or we should make a specific roadmap specifically for the purpose of simulating teenage readers then adjusting a section on each page for high-legibility based on those results.