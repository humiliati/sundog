# Kakeya Equianharmonic Probe - Operator Commands

```powershell
node scripts/kakeya-equianharmonic-probe.mjs
```

Primary outputs:

- `results\kakeya\equianharmonic-probe\manifest.json`
- `results\kakeya\equianharmonic-probe\probe.csv`

Out-of-register q = 13 sidecar; the workbench register (SUPPORTED_Q, spec,
tests) is untouched. No Euclidean claim.
