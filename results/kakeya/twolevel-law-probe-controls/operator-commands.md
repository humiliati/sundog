# Kakeya Two-Level Law Probe - Operator Commands

```powershell
node scripts/kakeya-twolevel-law-probe.mjs
```

Primary outputs:

- `results\kakeya\twolevel-law-probe-controls\manifest.json`
- `results\kakeya\twolevel-law-probe-controls\probe.csv`

Out-of-register q = 17/19 sidecars; the workbench register is untouched. No
Euclidean claim.
