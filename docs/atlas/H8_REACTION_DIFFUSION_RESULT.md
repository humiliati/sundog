# H8 Result — reaction–diffusion as a determine/resist shadow (substrate S3): **NULL**

> **2026-06-09.** Against `docs/atlas/H8_REACTION_DIFFUSION_PREREG.md`. Pointed the Shadow-Invertibility
> apparatus at a 2D Gray–Scott reaction–diffusion body. **The v1 determine/resist crossover does NOT
> hold** — a clean, multi-front NULL caught by the lab's own adversarial review *before* any claim. NOT
> public-eligible; frozen-as-portfolio. Attribution: Turing (1952); Gray & Scott (1983/84); Pearson (1993);
> the Shadow-Invertibility / charFun-spectrum laws (`SHADOW_CHARFUN_DETERMINE_RESIST_LAW.md`); Debye/Waller;
> Liu et al. (2013) for the look-alike panel.

## Headline — NULL, and the charFun law predicted it
The naive RD crossover (lossy ensemble shadow DETERMINES the morphology class, RESISTS the wavelength)
**fails**: the chosen continuous latent — the pattern **wavelength**, carried by a diffusion-scale knob
`s` — has a **finite centered mean**, so the lab's own **charFun law predicts it DETERMINES**
(concentrates under averaging by the law of large numbers), **not resists**. The apparent "resist" in the
first calibration was a **finite-K, under-powered-probe artifact**. The discrete "determine" half is
real but **trivial** (a single non-overlapping threshold). There is no genuine crossover for these two
RD quantities. This is consistent with — and a fresh confirmation of — the charFun law on a new substrate.

## The decisive test (the falsification)
A genuine charFun/Debye–Waller resist is **K-invariant**: the population's characteristic function decays,
so the wash is independent of how many subunits `K` you average (S0/S1/S2 satisfy this). A finite-mean
(determine-type) latent washed only by finite-K LLN slack has a half-life that **grows with K** — a bigger
ensemble *recovers* the latent. S3c is unambiguously the latter:

| K (subunits) | wavelength-recovery half-life λ\*_c |
|---|---|
| 1 | 0.1 |
| 2 | 0.5 |
| 8 | 1.0 |
| 32 | 2.0 |
| 128 | 3.0 (still 0.25 at λ=3) |

The half-life marches **outward** with K (post-histogram-fix frozen test corroborates: K=1→0.1, K=8→0.5,
K=64→1.5). The "K=1 washes most, the ensemble recovers" pattern is the LLN signature of a determinable
quantity — the exact opposite of a Shadow-law resist. **The earlier "K=1=0.0 ⇒ ensemble essential"
reading was backwards.**

## Pre-registered scorecard
| Gate | Result |
|---|---|
| G1 preflight-c (λ=0 wavelength recoverable) | PASS (cont₀≈0.70–0.73) |
| G2 preflight-d (λ=0 class recoverable) | PASS (disc₀=1.0) |
| **G3 continuous-resists** | **FAIL** — at the frozen DATA_SEED, n=160: `cont[λ=2.0]=0.215 > 0.10` (the n=64 calibration's clean 0.000 was an under-powered probe; Ridge makes the tail *worse* ⇒ genuine residual signal, dual-channel: radial 0.25 + histogram 0.23) |
| G4 discrete-determines | PASS but **TRIVIAL** — spots cc∈[10,37] vs stripes cc∈[1,2] (non-overlapping); a depth-1 threshold gives det=1.0 at every λ; ensemble averaging *cannot* cross the gap, so the lossiness axis never tests the discrete latent |
| **G5 CROSSOVER** | **FAIL** (KILL-1 central leak + KILL-3 not-lossiness: the "resist" is K-dependent LLN slack, not charFun decay) |
| G6 is-it-RD panel | balanced-acc ≈0.92 **but carried by the intensity histogram** (RD-vs-matched-GRF: hist-only=1.0, cc-only=0.51≈chance) — the C2 null is defeated by the marginal value distribution, not morphology, so it does not bite |

**Kill criteria triggered: KILL-1 (central leak survives a strong probe) and KILL-3 (resist is finite-K, not a lossiness/charFun effect).** As pre-registered, a kill is a bankable null.

## The arc (honest, including a premature positive I owned)
1. Built the S3 probe (batched Gray–Scott kinetics→feature library; S3c resist leg, S3d determine leg,
   B-panel, copied probe apparatus). Calibration at n=64 reported **CROSSOVER=True** and I relayed it as
   "clean." That call was **premature.**
2. Ran a 4-skeptic **adversarial review** (the standing discipline). It killed the crossover on four
   independent fronts: (a) the resist runs *backwards* to Debye–Waller (K-dependence); (b) at real probe
   power (n=160) G3 fails (0.215); (c) the determine is a single-threshold tautology; (d) **integrity
   bugs** — the cited `calibrate.json` was generated by *superseded* `gen_s3d` (proven by a 12/12 RNG
   fingerprint — a stale receipt) and `feature()` baked a per-batch `v.max()` histogram leak.
3. **Verified the decisive claim myself** (the lab verifies the reviewer too): the half-life-vs-K table
   above is my own clean run; the n=160 G3 failure reproduced at the frozen seed.
4. **Fixed the bugs** (per-field contrast-invariant histogram; folded C5 into the crossover gate;
   removed the stale `calibrate.json`) and re-ran — the NULL is **robust to the fixes** (the backwards-K
   is structural, not a bug). Locked it in the frozen test.

## What is genuinely true (the bankable content)
- **The charFun law's discriminator works on RD.** It correctly classified my continuous latent: a
  finite-mean quantity (wavelength via diffusion-scale) *determines*. My naive "RD wavelength resists"
  was falsified by the law's own prediction. This *extends the law's evidence*, via a clean negative.
- **The determine half extends to RD but trivially** — a topological class label survives a lossy shadow
  because it is a non-overlapping invariant; this is real but does not *test* the lossiness axis.
- **The look-alike panel separates by marginal statistics, not mechanism** — "is-it-RD" from a single
  frozen snapshot is carried by the value histogram (RD bimodal vs GRF Gaussian vs CH ±1), echoing the
  pre-registered single-snapshot wall (arrested Cahn–Hilliard mimics Turing without a time series).

## What a genuine RD crossover needs (the v2 design, owner-gated)
The fix follows directly from the charFun law: the continuous latent must enter through a **charFun-
decaying channel — a PHASE** (as S0's `cos(2π(c+λξ)t)` and S1's winding-phase do). The motivated v2,
mirroring **S1** on a real Gray–Scott body (and the recon's H8-A defect-topology idea done right):
- **continuous (resist):** the pattern's **spatial phase** (stripe/spot registration), read with
  phase-sensitive features → charFun-washes under ensemble phase-jitter;
- **discrete (determine):** the **defect topological charge** (dislocation winding number) — a
  non-trivially-separable invariant that fixes the determine-tautology;
- a **stronger null:** an intensity-CDF-matched (not power-only) surrogate so KILL-4 can actually fire.
Pre-registered separately before any build.

## Honest boundaries
- Synthetic-only; the continuous latent tested was a *diffusion-scale* knob, a thinner proxy than the
  full (F,k) kinetics — the kinetics/phase version is owed (v2).
- The Atlas catastrophe/jet machinery stayed OUT of scope as a read-out (correctly — it would be the H7
  Riemann-density mistake).
- Forward-only; the frozen test re-runs deterministically (byte-identical features).

## Files
- `scripts/reaction_diffusion_shadow.py` — the S3 probe (now an honest NULL-demonstrator: the
  `k_dependence` decisive test + the charFun verdict).
- `scripts/test_reaction_diffusion_shadow.py` — frozen test locking the NULL (the backwards-K signature +
  trivial-determine + determinism).
- `docs/atlas/H8_REACTION_DIFFUSION_PREREG.md` — the locked pre-reg (see the correction note on the
  FROZEN-CONSTANTS block).
