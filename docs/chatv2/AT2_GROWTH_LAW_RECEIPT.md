# AT-2 — Growth-Law Receipt

> 2026-07-03. Owner-run batch (at2_growth_g200/g300 exports + lock_v5_k5/k6 twins) +
> `scripts/at2_growth_law.py` per the frozen `AT2_GROWTH_LAW_SPEC.md` v1.1.
> Artifacts: `results/proof/at2-g{200,300}/` (npz + `at2_growth_law.json`),
> `results/proof/c1-k{5,6}-twin/`. **Non-promotional; 32×32 truncation measurement;
> no ∞-dim NSE claim; banked receipts untouched.**

## Verdict: `NO_GATE_READ` at both regimes — the frozen ≥3-cell rule refused, correctly

| G | τ=250 | τ=500 | τ=1000 | τ=2000 | included |
|---|---|---|---|---|---|
| 200 | K_min=1 ✓ | K_min=1 ✓ | **excluded (damp 0.000, atom 0.507)** | **excluded (atom 1.000)** | 2/4 |
| 300 | K_min=2 ✓ | K_min=2 ✓ | **excluded (damp 0.530)** | **excluded (damp 1.000)** | 2/4 |

Value control: K_min = 1 (G=200) / 2 (G=300). State-proxy crossing K_state = 1 / 2.
Liveness intact (damp = 0.300/0.269 exactly at the included cells; atom 0.000 there).

## The finding: lookahead-max is a short-horizon instrument on this cell

The τ-grid did not fail randomly — it degenerated by two named mechanisms, one per regime:

- **G=200 (near-periodic): AT-1's threshold-atom degeneracy is horizon-dependent.**
  Clean at τ ≤ 500 (atom mass 0.000); at τ = 1000 the atom holds **0.507** of the mass;
  at τ = 2000, **1.000** — every long window catches the same recurring burst peak, and
  the calibrated quantile lands on the atom (damp → 0). The **threshold-atom clearance
  check registered from AT-1 fired exactly as designed** — its first live catch.
- **G=300 (aperiodic): damp saturation.** Over long horizons every window bursts
  (damp 0.530 → 1.000) — the q = 0.70 max-quantile action degenerates to "always."

So the growth question **cannot be asked above τ ≈ 500 with the max-form objective on
this cell** — the instrument dies before the horizon stretches. (AT-1's mean-form lesson
extends: a mean/fixed-horizon objective would not saturate this way — an AT-2b option.)

## Reported reads (non-gate, per the frozen rules)

1. **Cross-regime, matched τ:** K_min(J_q) gains **+1 at both included τ** (1→2 at 250
   and 500) — the M2 direction (Foias–Temam / Jones–Titi K\*↑G), and numerically at the
   noted-not-gated ≈1.3-flavored ratio (1→2). Reported only: each regime has < 3 included
   cells, so no gate arm fires — including this one.
2. **The vacuity-flavored pattern:** at every included cell, **K_min(decision) =
   K_min(value) = K_state** (all 1 at G=200; all 2 at G=300). Wherever measurable, the
   decision budget equals the state-reconstruction budget — the §3.6 collapse *pattern*,
   visible but not gate-fired. An honest prior for any AT-2b: the likely verdict on a
   denser in-band τ-grid is `AT2_COLLAPSE_VACUOUS`, not growth.
3. **M1 (growth in τ):** unobservable — the grid died before τ could stretch. No
   spacing side-read.
4. **Event sub-read (§2.5, the reconnection analog): `AT2_EVENT_FLAT` at both regimes**
   (gap = 0 at the single evaluable cell each). The 2D-analog prediction found no signal
   in the evaluable range; the 3D parked lead gains no motion from this receipt.

## K\* bracket (the §5 debt — closed with a typed wall)

`lock_v5_k5` / `lock_v5_k6`: **`DEFERRED_COVERAGE`**, with
`max_evaluated_minority_fraction = 0.0` — at signature dim 50/72 the twin certificate
finds **no evaluable neighborhoods at all** (the K=4 curse-of-dimensionality, now total).
Bracket: **K\* > 4, instrument coverage-walled at K ≥ 5** — a `NUMERICALLY_WALLED`-class
outcome (binding lesson 4), not a witnesses-vanished read. The pre-registered companion
ran; its honest yield is the wall's location.

## Disposition (owner's call — none taken)

1. **AT-2b amendment (new registration, not a rescue):** τ-grid inside the usable band
   (e.g., {100, 250, 400, 500, 750}) and/or the mean-form objective (AT-1's clean
   sibling). Honest prior per read #2: expect vacuous-collapse, which would itself be the
   §3.6 negative the anchor doc pre-registered — a completion, not a disappointment.
2. **Accept the bounded read:** on this cell, decision-vs-state budget separation is
   unmeasurable above τ ≈ 500 and absent below it. Rank A closes with AT-1 + AT-2 both
   filed as typed instrument findings.
3. **σ_modes registration** (owner decision #2, restated per the spec): the receipts
   now weigh *against* registering σ_modes as a schema filtration — no growth was
   measured and the visible pattern is budget-collapse, not grading. Registration on
   receipts, not intentions ⇒ defer.

Cross-refs: `AT2_GROWTH_LAW_SPEC.md` v1.1 (frozen prereg), `AT2_HARNESS_SIGNOFF_REQUEST.md`
(stamped), `AT1_BOUNDARY_LAYER_RECEIPT.md` (the atom mechanism, here horizon-resolved),
`PDE_C1_SEPARATION_STATEMENT.md` §5 (the K-window companion, now run),
`NSE_ATTRACTOR_TAIL_HYPOTHESES.md` AT-2/F1/§2.5.
