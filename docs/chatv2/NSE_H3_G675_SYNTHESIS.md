# NSE-H3 Matched-Re G=675 Synthesis (internal; the closed receipt set, in one object)

> 2026-07-10. **Internal synthesis only; no public or external-review claim is
> licensed here.** No new claims, no new gates — this document pins what the
> forcing-axis arc's receipts already say, so the ledger cannot drift. Every
> load-bearing receipt is linked. Non-promotional; finite-Galerkin 32×32,
> proxy-relative (`NSE-H1-PROXY-ONLY` carried), sampled support, no C1 promotion.

## 1. One-line state

**The matched-Re forcing move produced the lane's best regime-2 specimen: the
decision is decodable from the 2-dimensional forcing coordinate alone; the state
is non-marginally free through 72-dimensional shadows; the fiber witness clears
its gates on the coherent mode band; and the full-support fiber walls are
measured attractor geometry, not apparatus failure.**

## 2. Receipt table

| entry | verdict | decisive measurement | forbids future readers from saying |
| --- | --- | --- | --- |
| v1 admission, (k_f=3, G=200) ([receipt](NSE_H3_ADMISSION_RECEIPT.md)) | `NSE-H3-INPUT-UNPOWERED` (steady state) | E_low constant 0.321; atom mass 1.000 auto-typed the fixed point in ~5 min | that fixed-G k_f moves are testable — Re_f ∝ G/k_f³ exits the chaotic window |
| v1.1 matched-Re admission (k_f=3, **G=675**, Re_f=25 held) | `H3_CELL_ADMITTED_PROBE_TIER` | damp 0.333, aperiodic envelope [0.40, 1.15] | that the forcing axis is untestable — Re-matching is the honest single move |
| frozen locks, 50k+200k ([receipt](NSE_H3_ADMISSION_RECEIPT.md), v1.2) | **`NSE-H3-INCONCLUSIVE_COVERAGE`** (final) | candidate coverage 0.4588 → 0.4692 across 4× samples — N-flat | that more N clears the fiber gate at this cell |
| Approach A, density-stratified ([receipt](NSE_COVERAGE_ADAPTIVE_RECEIPT.md)) | **`NSE-H3-COVERAGE-SLIVER`** | dense-count median **0**, f = 0.036; regression: bit-identical to frozen on both anchors | that the coverage wall is bookkeeping — an apparatus that cannot manufacture coverage confirmed the dense fibers are absent |
| Approach B v1, tercile guard ([receipt](NSE_REGIME_CONDITIONED_EPS_RECEIPT.md)) | **`NSE-H3-APPARATUS-REJECTED-B`** | guard fired on the certified anchor (natural U-shaped disagree ~0.06 > tol 0.03) | that a raw-spread scale guard can gate a relative-fidelity read |
| Approach B v2, inflation-shell ([receipt](NSE_REGIME_CONDITIONED_EPS_V2_RECEIPT.md)) | `NSE-H3-FORCING-GENERAL-RELATIVE` (**fenced**) | SHELL = 0.099% of witness pairs; relative radius mostly *smaller* than frozen | that relative resolution revealed scale-invariant structure — the positive is CORE-dominated; the operative change was dropping the s_pos gate |
| structure dig-in ([doc](NSE_H3_G675_STRUCTURE_DIGIN.md); exploratory) | nucleus carries **both halves** | dense core disagree 0.0249 (tightest); 80.8% of dense pairs state-separated; mode-band coverage 0.690 | that the SLIVER meant no structure — it meant small |
| mid-E sub-regime ([receipt](NSE_H3_MIDE_SUBREGIME_RECEIPT.md)) | **`NSE-H3-FORCING-GENERAL-MIDE`** | within-band coverage 0.6863 ≥ 0.50; `TWIN_STATE_CERTIFIED` (82,547 pairs); paired disagree 0.0218 — every frozen gate clears | that no gate-clearing forcing-axis positive exists; also that it covers the tails (it is scoped to the mode band, disclosed as the densest region) |
| global gauge ([receipt](NSE_H3_GLOBAL_GAUGE_RECEIPT.md)) | **`NSE-H3-GLOBAL-REGIME2-NONMARGINAL`** | FVE 0.6322 (residual 0.368 vs anchors' 0.001/0.008); eq-wt median R² 0.0039; control 0.887 powered, strongest in the high-E tail | that C1's regime-2 is inherently marginal — the marginality was a property of the anchor cells, not the phenomenon |
| K-sweep + K=1 addendum ([receipt](NSE_H3_KSWEEP_RECEIPT.md)) | **`NSE-H3-KSWEEP-WINDOW-MEASURED`** | m_det bracket **> 6**; window **K ∈ {1..6}**; control floor = **the forced mode (0,3) alone** (2 dims, acc 0.824); eq-wt ≈ 0 at every K | any m_det claim at this cell; that the control floor is above the forcing coordinate; that FVE(K)'s shape is meaningful (target composition — only the bracket, the eq-wt floor, and the control curve are invariant) |

## 3. The reading: two gauges, one geometry

The arc's central fact is an inversion. The matched-Re attractor is ~3.7:1 wide
in signature energy — and that **same width** is:

- **the obstruction in the fiber gauge** (sparse ε_K-fibers → frozen DEFER, A
  SLIVER, B fence, mode-band-only MIDE scope), and
- **the signal in the global gauge** (FVE 0.63, the non-marginal state freedom).

And the two ends of the witness are anchored to different objects:
**the decision is anchored to the forcing** (the (0,3) coordinate alone decodes
the action at 0.824; saturation by K≈3) while **the state is anchored to the
attractor** (free through 72-dim shadows, typical component unpredictable at
every K). This is the regime-2 asymmetry in its sharpest measured form — the
static sibling of AT-3's dynamic ladder (`K_dec = 1 < K_sync = 2 < K* > 4`).

## 4. What the discipline earned (instrument ledger)

- **Coverage gates fired honestly four times** and were right each time (the
  dig-in confirmed structure-present-but-small, exactly what they said).
- **Regression-before-read rejected one apparatus (B v1) and validated three**
  (A bit-identical; B v2 by construction; global gauge on both anchors) — no
  apparatus read the open cell without first reproducing the certified anchors.
- **The raw-sample export paid for itself five times** (B v2 reprocess, dig-in,
  mid-E, global gauge, K-sweep — all post-processing; the K-sweep needed one
  capture instead of five integrations).
- **Pre-registered violation clauses caught real effects** (FVE non-monotonicity
  → target composition; the UNDERCOVERED null was live and didn't fire).

## 5. Do-not-reopen list

1. **No full-support fiber reopens at G=675 without a new resolution mechanism**
   — frozen/A/B closed it three ways; the wall is measured geometry.
2. **No fidelity-rule change without a control-validated guard** (B v1's lesson);
   any B-style read must pass the inflation-shell class of guard on the anchors.
3. **The MIDE positive stays scoped to the mode band**; the B positive stays
   fenced (CORE-dominated); neither is a full-attractor fiber claim.
4. **K=1 stays fenced**: the label's band contains the forced mode, so partial
   self-correlation is expected — the receipt's claim is the powered margin from
   a single coordinate, nothing more.
5. Carried mandates: atom clearance, balance/coverage gates, blocked splits,
   permutation controls, regression-before-read — binding on any successor.

## 6. Next-work menu (owner-gated; nothing recommended as urgent)

- **Grid-48 N-refinement at G=675** (the H2 discipline at the non-marginal
  cell): CFL will be tighter than G=300/48 — dt=0.005-class, ~2–4 h × 2,
  owner-run; would harden the lane's best specimen the way H2 hardened the
  anchors.
- **AT-3 dynamic ledger at G=675**: K_sync at the non-marginal cell — the sharp
  question is whether the maintained ledger stays cheap (K_sync ≈ 2) where the
  static state is this free. New integrations, own registration.
- **k_f=4 at matched Re** (G=1600, Re_f=25): the second forcing-geometry point;
  probe-first per the v1.1 pattern.
- Public/promo work: out of scope; `docs/chatv2/` stays no-publish; any keeper
  language goes through the state-crossover handoff's keep-true box.

Cross-refs (load-bearing set): `NSE_H3_KF3_SCOPE.md` (+§7/§8),
`NSE_H3_ADMISSION_RECEIPT.md`, `NSE_COVERAGE_ADAPTIVE_APPARATUS_{SCOPE,SPEC}.md`
+ `NSE_COVERAGE_ADAPTIVE_RECEIPT.md`, `NSE_REGIME_CONDITIONED_EPS_{SCOPE,RECEIPT}.md`
+ `..._V2_{SPEC,RECEIPT}.md`, `NSE_H3_G675_STRUCTURE_DIGIN.md`,
`NSE_H3_MIDE_SUBREGIME_{SPEC,RECEIPT}.md`, `NSE_H3_GLOBAL_GAUGE_{SPEC,RECEIPT}.md`,
`NSE_H3_KSWEEP_{SPEC,RECEIPT}.md`, `NSE_POST_AT_HYPOTHESES_SLATE.md` (H3's origin),
`NSE_ATTRACTOR_TAIL_SYNTHESIS.md` (the closed predecessor arc).
