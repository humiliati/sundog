# SUNDOG_V_BODHISATTVA

*Lane spine: the self-correction-dynamics program. Christened 2026-07-03 after its gate discharged —
the Angle-4 fixed-point sentence survived rigor, sharpened (Percival Track-C v1–v4 + the B1/B2
empirical leg are this lane's founding receipts). The opening deliverable is the claim decomposition
below: which parts of the spine CAN be litpassed against meaningfully, against what, and what kills
them. The LP slate gates everything public.*

Status: **OPENED 2026-07-03. Spine + litpass slate only; no new experiment.** The name was banked as
a known **slop-attractor** (a Fiverr reviewer once labeled the owner's h-of-x work "the bodhisattva"
via GPT slop), so the register discipline below is constitutional, not decorative.

---

## The thesis (falsifiable form)

> **Self-correction is a coverage phenomenon.** An agent's ability to correct its own objective from
> experience is decided by three separable quantities — the context-invariance gap between true and
> proxy objectives, the probe mass its correction rounds place on the disagreement region, and the
> ordering of its simplicity prior — and the corrigible fixed point of the deploy-correct loop is
> absorbing **iff** coverage is sustained under the corrigible deployment itself. Where coverage
> cannot reach (unsampleable defect, inverted prior), only an external signal ("grace") recovers the
> true objective, and its power is exactly its coverage.

**Refined coverage form (adopted from LP-2, 2026-07-03):** coverage acts EITHER as probe mass on the
disagreement region D directly, OR — when the true objective is algebraically self-reducible — as mass
on the honest region ¬D plus structure linking ¬D to D (oblivious defense). The strict "mass on D" is
the OPAQUE-objective special case; objective structure is a second recovery route (bridges LP-5).

Falsified by: an internal discriminator that separates corrective from corrupting updates without an
invariance gap; a repair mechanism that recovers V with NEITHER coverage NOR objective structure (LP-2's
oblivious defense does NOT do this — it is coverage of ¬D + structure — but a structure-free,
coverage-free repair would kill the law); a corrigible fixed point that is absorbing under an inverted prior
without sustained coverage.

## What this lane is NOT (the slop fences)

- **NOT a compression of the Buddhist literature into a formal buddha.** That direction was graded
  the slop door on day one and is declined. No claims about Buddhist doctrine are made anywhere.
- **The myth is a register, not evidence.** Perceval/Campbell (the unasked question, the Wasteland
  wandering, Trevrizent) supplied working intuitions — several paid out as formal objects (the
  wandering = max-entropy proxy-decoupling; the hermit = full-support unannounced probes) — but no
  claim is made against the Grail corpus, and medievalist outreach stays GATED (Percival B5 rule).
- **No deception-detection product claims.** The deceptive-W′ machinery is a model; nothing here
  detects deception in real systems. PROMOTE_GATE language discipline binds.
- Public-surface naming is owner-gated; "bodhisattva" is the internal codename until decided.

## Founding receipts (banked, committed)

| leg | verdict | anchor |
| --- | --- | --- |
| v1 frontier (gap × budget × simplicity) | `TC_SELF_CORRECTION_MAPPED` | `percival/PERCIVAL_TRACKC_SELF_CORRECTION_*` |
| v2 coverage law (adversarial placement) | `TCV2_GRACE_IS_COVERAGE` | `percival/PERCIVAL_TRACKC_V2_*` |
| v3 pairing theorem (noise layer) | `TCV3_CRISPNESS_IS_PAIRING` | `percival/PERCIVAL_TRACKC_V3_*` + Lean `PercivalNoisyMargin.lean` |
| v4 fixed-point gate (three regimes) | `TCV4_GATE_DISCHARGED_SHARPENED` | `percival/PERCIVAL_TRACKC_V4_*` + Lean `PercivalFixedPoint.lean` |
| B1/B2 empirical leg (Pythia ladder) | `B1B2_REFUTED` (anatomy: law calibrated ~3%, floor exactly 0; dial + T-ratio extrapolations died) | `percival/PERCIVAL_TRACKC_B1B2_*` |

14 machine-checked theorems across the two Lean modules (audit 8584 green), three MC harnesses, one
real-weights run. Sibling litpass already closed: B1/B2's (`PERCIVAL_TRACKC_B1B2_LITPASS_MEMO.md`).

---

## THE LITPASS SLATE (the opening deliverable)

Each entry: the spine claim → the literature it must survive → pinned anchors (VERIFIED = checked
this pass; TK = training knowledge, re-verify before citing) → collision risk → the delta if it
survives → what kills it. **LP-1 and LP-2 carry two MUST-READS each; no public surface until all
five entries have house-style litpass verdicts.**

### LP-1 — The deceptive-capture region vs ELK and the simplicity-of-deception arguments — **VERDICT 2026-07-03: SURVIVES, NARROWED** ([`BODHISATTVA_LITPASS_MEMO.md`](BODHISATTVA_LITPASS_MEMO.md))

> Closed against arXiv:2606.12268 (verified). Their impossibility is BEHAVIOURAL ("no feedback strategy
> depending only on agent behaviour ... produces an honest agent with certainty") and CID-based — 0
> hits for simplicity/description-length. They own the QUALITATIVE inseparability (= our v2 Q1; concede,
> cite, do not claim as ours). We own the QUANTIFICATION (margin λ·ΔL, capture region) + the coverage
> escape, which is COMPLEMENTARY (it is the non-behavioural access their theorem brackets out) + the
> machine-checked form. Register: "quantify a known inseparability + map its exits," never "discovered."


- **Claim under test:** v1's capture region {inverted simplicity prior ∧ defect-unsampled ∧ λ>0} and
  its margin algebra (deception wins by exactly λ·ΔL when observationally identical on the sample).
- **Against:** ARC's Eliciting Latent Knowledge report (Christiano/Cotra/Xu — the human-simulator vs
  direct-translator problem IS a simplicity-ordering selection between observationally-identical
  reporters; their own simplicity/speed-prior counterexamples); Hubinger et al. deceptive-alignment
  simplicity arguments (TK); **MUST-READ: "The Impossibility of Eliciting Latent Knowledge"
  (arXiv:2606.12268, June 2026 — VERIFIED to exist; could own our inseparability claims wholesale
  in a stronger formal form).**
- **Delta if survives:** the crisp selection margin + exact capture region + the coverage escape +
  machine-checked anchors — ELK's difficulty as a two-line inequality with a map of the exits.
- **Killed by:** the impossibility paper (or the ELK report itself) already stating the
  region/margin form; then we demote to "toy restatement of ELK with Lean anchors" — still citable,
  not a discovery.

### LP-2 — The coverage law + scheduled-audit-zero vs control, backdoors, and oblivious defense — **VERDICT 2026-07-03: SURVIVES, REFINED** ([`BODHISATTVA_LITPASS_MEMO.md`](BODHISATTVA_LITPASS_MEMO.md))

> Closed against arXiv:2411.03279 (verified) + GKVZ arXiv:2204.06974. Oblivious defense is COVERAGE IN
> DISGUISE (queries many correlated random points + aggregates via random self-reducibility) — confirms
> "grace = coverage," does not break it; its lower bound ("mitigation cannot be distribution-free")
> strengthens the converse. TWO CONCESSIONS ADOPTED: (1) recovery can come from coverage of ¬D + objective
> self-reducibility, not only probe mass ON D — the strict v2 letter is the OPAQUE-objective case (refined
> form adopted in the thesis above; bridges LP-5); (2) whitebox ι (detectability) can be cryptographically
> nullified (GKVZ), yet recovery still succeeds — the interp arm carries that ceiling. Run LP-5 before any
> public statement of the refined law.


- **Claim under test:** v2's `recovery = probe mass on D` (mechanism-independent), announced-audit
  zero, self-simulation null; the whitebox arm's ι (inspectability) as just another probe support.
- **Against:** AI-control auditing (Greenblatt et al., untrusted-model auditing budgets, randomized
  audits — TK); **Goldwasser–Kim–Vaikuntanathan–Zamir, "Planting Undetectable Backdoors" (FOCS
  2022, arXiv:2204.06974 — VERIFIED): cryptographically undetectable backdoors ⇒ ι can be driven to
  ~0 even white-box — an upper bound our whitebox arm must carry explicitly;** sleeper agents /
  password-locked models (TK — empirical adjacency for defect-at-unsampled-contexts);
  **MUST-READ: "Oblivious Defense in ML Models: Backdoor Removal WITHOUT Detection" (STOC 2025 —
  VERIFIED to exist): repair without finding D is a direct escape route from "grace = coverage" that
  our toy does not model. Either bound it (their mitigation is itself a full-support intervention =
  coverage in disguise?) or concede the law's scope.**
- **Delta if survives:** the unification (samples ≡ inspectability ≡ coverage, one quantity) + the
  audit-scheduling zero as a crisp theorem-shaped statement + the self-simulation null.
- **Killed by:** control literature already carrying the coverage-rate math (plausible — randomized
  auditing analysis is their bread and butter); oblivious defense breaking mechanism-independence.

### LP-3 — The three-regime fixed points vs performative prediction — risk: MEDIUM-HIGH

- **Claim under test:** v4's classification (bodhisattva/wirehead/wandering) of the deploy-correct
  chain; coverage as the contraction variable.
- **Against:** Perdomo et al. 2020 (arXiv:2002.06673 — RRM converges to performative stability iff
  sensitivity/convexity conditions hold; non-convergence otherwise — TK for exact constants);
  **Brown–Hod–Kalemaj, "Performative Prediction in a Stateful World" (AISTATS 2022,
  arXiv:2011.03885 — VERIFIED): necessary AND sufficient RRM convergence conditions, stateful** —
  our wandering regime is plausibly their non-convergence in two-point clothing.
- **Delta if survives:** coverage (a probe/information quantity) as the contraction variable rather
  than loss-geometry constants; the asymmetric noise-robustness (falls deterministic, recoveries
  probit-thinned); the exact absorbing characterizations. The discrete chain is a *different
  axis* — theirs is loss geometry, ours is information access — but that has to be SHOWN, not said.
- **Killed by:** a stateful-performativity result already parameterizing convergence by an
  information/coverage quantity.

### LP-4 — Corrigibility-not-absorbing vs the basin-of-corrigibility debate — risk: MEDIUM

- **Claim under test:** v4's sharpest consequence — under an inverted prior, corrigibility is NOT
  absorbing; one uncovered correction round topples it, at any noise level; sustained coverage, not
  one-time redemption.
- **Against:** Christiano, "Corrigibility" (ai-alignment.com — the broad-basin-of-attraction claim —
  VERIFIED); **the live counter-thread: "The corrigibility basin of attraction is a misleading
  gloss" (LessWrong — VERIFIED) and CAST "Corrigibility as Singular Target" (arXiv:2506.03056, June
  2025 — VERIFIED);** Soares et al. 2015 "Corrigibility" (TK).
- **Delta if survives:** a machine-checked micro-instance ON the anti-basin side with EXACT
  conditions (prior ordering + coverage) — the debate is informal; a formal toy that says precisely
  *when* the basin fails would be a real contribution at honest scope (two hypotheses, one channel).
- **Killed by:** CAST or the counter-thread already formalizing the non-absorbing condition; or the
  toy's scope being too thin to bear on the debate (a real possibility — must be stated as a
  micro-instance, never "corrigibility is unstable, proved").

### LP-5 — The invariance discriminator vs IRM/ICP and their failure results — risk: MEDIUM

- **Claim under test:** Angle-2's sussed core — the ONLY internal discriminator between corrective
  and corrupting updates is the context-invariance gap (F2: coherence, prediction-error, ontological
  robustness all reduce to invariance); corrective bits are extractable from distributional
  divergence under a structural-simplicity prior.
- **Against:** IRM (Arjovsky et al. 2019 — TK); Invariant Causal Prediction (Peters–Bühlmann–
  Meinshausen 2016 — TK); **Rosenfeld et al. 2021 "The Risks of IRM" (TK — already flagged in the
  lane's own notes: IRM's OOD guarantees are weak, so "self-correction via IRM" is only as strong as
  they are);** causal-invariance objective-identification follow-ons (unsurveyed).
- **Delta if survives:** the reduction claim (all internal discriminators ≤ invariance) stated and
  toy-tested; the three-tier gap×budget frontier.
- **Killed by:** the reduction being too strong (most likely failure — it needs weakening to a
  conjecture or a proof before any public statement); or the invariance-discovery literature already
  owning the objective-discovery framing.

### LP-6 — Pairing/evaluation statistics — CLOSED

Already litpassed and demoted honestly (`PERCIVAL_TRACKC_B1B2_LITPASS_MEMO.md`: classical law +
Miller 2024 + Kotawala 2026; our deltas = dial, floor audit, T-contrast, Lean anchors — and the
B1/B2 run then killed the dial and T-ratio extrapolations while confirming the law). Reuse; no new
pass.

### Not litpass-able (and therefore not claims)

The Lean modules (artifacts — they are the *form* of our derivations, not discoveries); the verdict
tokens and toy constructions (internal receipts); the myth register (fenced above); the
"self-corrective buddha" phrase itself (a codename, not a thesis).

---

## Gates + sequencing

1. **LP gate (open):** run LP-1..LP-5 as house-style litpass verdicts — the two MUST-READS first
   (arXiv:2606.12268; the STOC 2025 oblivious-defense paper), since each can demote a whole leg.
   Output: `BODHISATTVA_LITPASS_MEMO.md`, BOXSEL house style, time-stamped gap claims.
2. **Only after LP:** decide surviving-delta probes (candidates, NOT registered: the LP-2
   oblivious-defense bound; LP-4's basin micro-instance write-up; LP-5's reduction-claim weakening).
   Any public surface (page, post, outreach) is gated on the memo AND owner sign-off.
3. The B1/B2 open threads (manifest extension, real-checkpoint dial follow-ups) stay in Percival's
   ledger; this lane inherits them by reference only.

## Cross-links

Parent: [`SUNDOG_V_PERCIVAL.md`](SUNDOG_V_PERCIVAL.md) (Track-C receipts live in `docs/percival/`).
Umbrella: [`SUNDOG_V_CAUSAL_ACCESS.md`](SUNDOG_V_CAUSAL_ACCESS.md) (the base-update channel is a
fifth face of the access thesis: who can reach the correction loop's sampling distribution).
Machine-checked: `sundogcert` `PercivalNoisyMargin.lean`, `PercivalFixedPoint.lean` (+ the general
Percival anchors). Method kin: determine/resist ([[project_sundog_suffstat_order_slate]] in the
owner's notes), the coverage/probe framing shared with the OR lane's MEASURABLE≠ENFORCEABLE split.
